name: Test workflow
on: [workflow_dispatch]
jobs:
  hello-world:
    runs-on: ubuntu-latest
    steps:
      - name: print hello world
        run: echo Hello world
      - name: print my name
        run: echo alex
